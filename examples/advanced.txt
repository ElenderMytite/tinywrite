# Test advanced scenarios

# Multiple variable assignments and operations
x = 5;
y = 10;
z = 15;
(+ x y z);

# Swap variables (from var_swap.txt)
p = 100;
q = 200;
p q = q p;
p;
q;

# Complex computation
n1 = 10;
n2 = 20;
n3 = 30;
result1 = (+ n1 n2);
result2 = (- n3 result1);
result1;
result2;

# Nested operations
outer = (+ 2 (- 10 3));
outer;

# Comparison with variables
x_val = 15;
y_val = 8;
(> x_val y_val);
(<= y_val x_val);
(== x_val x_val);
