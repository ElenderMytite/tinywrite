# Test expressions with variables and operations
# Assign result of expression
result = (+ 5 3);
result;

# Use variables in expressions
a = 10;
b = 20;
(+ a b);
(- b a);
(* a b);

# Nested expressions
c = (+ (- 10 5) (* 2 3));
c;

# Expression with variables
d = 7;
e = 3;
(> d e);
(<= e d);
